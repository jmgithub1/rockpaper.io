// a function that gathers the users moves into a variable
let userMove = prompt ("Rock, Paper or Scissors?")

function userChoice (userMove){
    return userMove
}
alert(choice(userMove))







// a function that randomly generates a computer move








// a function that compares the two moves to see who has won the round.